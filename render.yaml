services:
  - type: worker
    name: tg-ai-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TG_BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false